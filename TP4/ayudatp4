se puede usar telnet o net como cliente para que no haga falta crearlo
usar de base el servidor con socket de las clases
cambiarle el while que tiene un read (sacarselo) para que cuando termine de leer termine el servidor
parsear (cortar el texto usado) para obtener la palabra a buscar, hay que usar un caracter para usar como delimitador (del strntok o strtok)


ejemplo de reuerimiento a una pagina GET /index.html HTTP/1.0


gethostbyname()

agregar al cliente de prueba el getho.. para sacar la ip del sitio y parsear lo que devuelve